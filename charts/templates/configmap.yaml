apiVersion: v1
kind: ConfigMap
metadata:
  name: sonarr-genre-tagger-parameters
data:
  config.yaml: |
    tagging:
        drop:
            {{- range .Values.tagging.drop }}
            - {{ . }}
            {{- end }}
        replacements:
            {{- range $key, $val := .Values.tagging.replacements }}
            {{ $key }}: {{ $val }}
            {{- end}}
